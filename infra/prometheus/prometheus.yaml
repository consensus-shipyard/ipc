global:
  scrape_interval: 30s
  evaluation_interval: 25s

scrape_configs:
  - job_name: "prometheus1"
    static_configs:
      - targets: [
        "localhost:9184",
        "localhost:9185",
        "localhost:9186",
        "localhost:9091",
        "localhost:9092",
        "localhost:9093",
        "localhost:9090"
      ]
