[package]
name = "fendermint_actor_storage_adm_types"
description = "Storage ADM actor types and interface"
license.workspace = true
edition.workspace = true
authors.workspace = true
version = "0.1.0"

[features]
default = []


[dependencies]
serde = { workspace = true, features = ["derive"] }

