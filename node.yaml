# node-init.yaml

home: "/Users/karlem/.node-ipc"
subnet: "/r31337/t410fobdhc7jsv7fekdof23tvojgufaw7vulodh2rj4a"
parent: "/r31337"

key:
  wallet-type: evm
  # exactly one of these two:
  # path: "/path/to/keystore.json"
  private-key: "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"

# Optional CometBFT overrides (only fields you list get merged in)
cometbft:
  consensus:
    timeout_commit: "5s"
  rpc:
    laddr: "tcp://0.0.0.0:26657"

# Optional FenderMint overrides
fendermint:
  app:
    max_validators: 100

# JoinConfig — only valid for collateral‐mode subnets
# join:
#   from: "/r31337/t410fobdhc7jsv7fekdof23tvojgufaw7vulodh2rj4a"
#   collateral: 10
#   initial-balance: 50

# Now a flat GenesisConfig block:
genesis: !create
  base-fee: "1000"
  power-scale: 0
