name: Mono CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '**'
    # To add ready_for_review as a trigger we need to list all the defaults.
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review


jobs:
  ipc:
    uses: ./.github/workflows/ipc.yaml
    secrets: inherit

  fendermint:
    uses: ./.github/workflows/fendermint.yaml
    secrets: inherit
    needs: [ipc]
